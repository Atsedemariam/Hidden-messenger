name: hidden_messanger
description: "A new Flutter project."
publish_to: 'none' 

version: 1.0.0+1

environment:
  sdk: ">=3.6.0 <4.0.0"  
  
dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8 
  font_awesome_flutter: ^10.0.0
  image_picker: ^1.1.2 
  flutter_lints: ^5.0.0
  firebase_core: ^2.18.0 
  firebase_auth: ^4.0.0 
  cloud_firestore: ^4.17.5

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
